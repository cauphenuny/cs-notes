return f(x);
不含其他语句

frac转尾递归：
```cpp
int frac(int n) {
	if (n == 0) {
		return 1;
	}
	return n * frac(n - 1);
}
frac(n);
```

```cpp
int frac(int n, int ans) {
	if (n == 0) {
		return 1 * ans;
	}
	return frac(n - 1, ans * n);
}
frac(n, 1);
```