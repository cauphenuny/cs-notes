### 取指周期
PC -> MAR
1 -> R
M(MAR) -> MDR
MDR -> IR
OP(IR) -> CU
(PC)+1 -> PC

### 间址周期
Ad(IR) -> MAR
1 -> R
M(MAR) -> MDR
\[MDR -> Ad(IR)\]

---

## 多级时序系统

- 机器周期：所有指令执行的基准时间，可以是一次访存时间
- 时钟周期：略

![[cycle.png]]