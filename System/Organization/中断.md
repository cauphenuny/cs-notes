使用屏蔽字屏蔽中断请求：
e.g. 

| 中断源 | 屏蔽字    |
| --- | ------ |
| A   | $1111$ |
| B   | $0111$ |
| C   | $0011$ |
| D   | $0001$ |
效果：执行 $A$ 时屏蔽所有，执行 $B$ 时屏蔽 $CD$，etc.

![[intr.png]]
采用屏蔽技术之后可以动态改变处理优先级，而响应优先级通常是硬件设置好的，不便于改动

原理：想要先处理响应优先级低的中断，只需要调整高响应优先级中断的屏蔽字，使得响应高响应优先级中断后马上进入处理低响应优先级的中断，然后返回处理高响应优先级中断。